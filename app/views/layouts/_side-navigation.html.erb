 <div class="side-nav bg-dark">
    <%= link_to "Groups", new_group_path, class: 'active' %>
    <% current_user.member_ships.each do |membership|%>
      <%= link_to "#{membership.group.name}", group_path(membership.group), class: 'list-group' %>
    <% end %>
  </div>